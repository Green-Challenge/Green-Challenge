spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/green_challenge
        username: root
        password:
        hikari:
            minimum-idle: 1
            maximum-pool-size: 5
    mvc:
        hiddenmethod:
            filter:
                enabled: true
    jpa:
        show-sql: true
        database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
        properties:
            hibernate:
                format_sql: true
        hibernate:
            ddl-auto: none
            use-new-id-generator-mappings: false
server:
    port: 8080